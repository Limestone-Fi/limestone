{
  "name": "cancun",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "cancun/src",
  "// targets": "to see all targets run: nx show project cancun --web",
  "targets": {
    "build": {
      "command": "forge build --no-metadata"
    },
    "test": {
      "command": "forge test --no-metadata"
    }
  }
}
