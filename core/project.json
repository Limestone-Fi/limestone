{
  "name": "core",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "core/src",
  "// targets": "to see all targets run: nx show project core --web",
  "targets": {
    "build": {
      "command": "forge build --no-metadata"
    }
  }
}
